#!/bin/bash
# Скрипт для запуска Flutter приложения с контейнерами

echo "🚀 Запуск Flutter приложения с тремя контейнерами..."
echo "📱 Убедитесь, что у вас запущен эмулятор или подключено устройство"
echo ""

cd "$(dirname "$0")"

# Проверяем наличие Flutter
if ! command -v flutter &> /dev/null; then
    echo "❌ Flutter не найден. Установите Flutter и добавьте его в PATH"
    exit 1
fi

# Устанавливаем зависимости
echo "📦 Установка зависимостей..."
flutter pub get

# Запускаем приложение
echo "🎯 Запуск приложения..."
flutter run
